import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

const Cases = () => (
  <PagePlaceholder
    title="Cases"
    description="Case orchestration workspace coming soon. Use the dashboard to assemble documents while we finalize this module."
  />
);

export default Cases;
